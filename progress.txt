# Explore (trace) — Progress

## 2026-09-16 Verified: eRPC treats a legitimate zero as "no data"

Challenged on this ("0x0 is not empty bytes, 0x would be empty bytes"), so I
stopped relaying erpc's reading and verified it three ways.

1. UNIT TEST against eRPC's own `util.IsBytesEmptyish` at 3ec22bf8, the commit
   the fleet runs:
     "0x0" true   0x0 true   "0x000" true   "0x" true   [] true   null true
     "0x1" false  "0x2cd" false  "0x1744" false
   It strips a `0x` prefix, trims leading zeros, and calls what remains empty.
   So a real zero and absent data are the same value to that function.
2. SOURCE. `common/defaults.go` exempts the point state reads by hand —
   eth_call, eth_getBalance, eth_getCode, eth_getStorageAt,
   eth_getTransactionCount — under the comment "zero value is a real value, not
   absence." That is upstream acknowledging the conflation and working around
   it for five methods. It is also why `eth_getTransactionCount` never suffered:
   a zero nonce measures 105 ms.
3. CONTROLLED BEFORE/AFTER in production. valve added five methods to the
   accept list, which gave a natural experiment:
     uncle count on a FULL block      645ms -> 97ms
     tx count, empty block            608ms -> 100ms
     block receipts, empty block      611ms -> 88ms
     receipt by unknown hash (not added)   644ms -> 635ms
     block by unknown hash (not added)     644ms -> 631ms
     tx by index, empty block (not added)  645ms -> 638ms
     trace_block, empty block              609ms -> 634ms
   The last row is the tell: trace_block is on `markEmptyAsErrorMethods`, which
   bypasses the accept list, so listing it changes nothing.

CORRECTION TO THE CAUSE OF THE 502 FIX. valve's journal evidence beats my
bracket: chain-1 calls over 8s were logging `successfully forwarded` at
8998-9012ms and stopped dead at each edge's RESTART (01:33:47Z edge-a,
01:36:14Z edge-b), which ran the old binary and old config. My 01:35:50Z
synthesis sits between the two restarts, so it could only have hit the
still-unfixed edge-b. So the cause is a restart clearing accumulated state, and
the binary roll is demoted to a coincidence. A wedge that returns will not be
fixed by the new binary.

ALSO: erpc told me our deployed accept list already held those methods and that
adding them would be a no-op. The before/after above refutes that. Two sessions
reading the same config reached opposite answers; the measurement settled it.

Filing an upstream issue is NOT approved and has not happened. The draft is at
`valve-tech/probes/DRAFT-erpc-issue.md`, with the probe inlined rather than
pointing at an internal path.

## 2026-09-16 Evidence-quality correction: Caddy logs nothing useful here

`rpc-proxy.Caddyfile.tmpl` sets no `log` directive, so the container emits only
`http.log.error` entries and NO access lines. A 5xx that Caddy proxies through
leaves no trace. Proof: my own measured 9.1s and 18.1s 502s, made through this
proxy at 00:57-01:05Z, appear nowhere in its log.

So never read the absence of 502 lines there as evidence. Two things I nearly
mis-sold as evidence, and withdrew:
- "zero 502s in the hour after the fix" — consistent, but not probative.
- the last `/eth` error line at 01:38:42Z (0.2s) as a tighter floor — it is a
  client disconnect, not an empty-block timeout.

What the log DOES hold: 3,346 lines carrying `"status":502` are Caddy's own
errors (client EOF, connection reset, dial failure), spread across /sepolia
2048, /eth 615, /plsv4 350, /pls 333. Separately, 20 "aborting with incomplete
response, upstream one.valve.city" lines, 0.07-1.06s, across chains 1/369/
11155111, ending 00:38:39Z — too fast to be the empty-block timeouts.

The bracket therefore rests only on the two measurements that do prove
something: the last shim synthesis at 01:35:50Z, and empty block 25,971,025
answered with its chunk written at 01:46:04Z.

Redaction trap if we ever add an access log there: the rewritten URI carries the
RPC key (`/rpc/<key>/evm/1`), so a log format must exclude or scrub the request
URI. Every artifact I sent was scrubbed and then grepped for `vk_` first.

## 2026-09-16 Correction: the 502 was the relay, and the timings were its budgets

I wrote earlier that the gateway's 502 came from Cloudflare over a failed
origin. Half right, and the useful half is different. From erpc, with file and
line:

- eRPC NEVER returns 502. `determineResponseStatusCode` (erpc/http_server.go)
  has branches for 400/401/404/429/503 and returns 200 for every JSON-RPC
  application error. `ErrEndpointMissingData` is a 200 with an error body, by
  design.
- The 502 is valve's RELAY: `proxy-handle-single.ts:287` returns
  `res.status(502).json(rpcError(id, -32603, 'RPC proxy unavailable'))` after
  `maxUpstreamAttempts`. My batch measurement proves it — a batch came back
  HTTP 200 carrying exactly that per-item error object.
- A SINGLE call returned something else again: text/plain "error code: 502"
  with a Cf-Ray and no `x-powered-by`, which is Cloudflare's own page, not the
  relay's JSON. Caddy logged "aborting with incomplete response". So a
  single-call client gets an unparseable body where a batch client gets a
  proper JSON-RPC error.

The three timings were the relay's timeout budgets, not the node's work:
  eth_getBlockTransactionCountByNumber  not slow  3,000ms x 3 = 9,000   (9.1s)
  eth_getBlockReceipts                  slow      9,000ms x 2 = 18,000  (18.1s)
  trace_block                           slow      9,000ms x 2 = 18,000  (18.1s)
`isSlowMethod` counts eth_getBlockReceipts by membership and trace_block by its
`trace_` prefix.

The sweep itself is generic and shape-based: `networks.go:2250` refuses ANY
emptyish result whose method is not on `emptyResultAccept`, and
`util.IsBytesEmptyish` treats null, [], {}, "", 0x and 0x0 as empty.
`markEmptyAsError` is a second, stronger path that converts empty to an error
and bypasses the accept list; only `trace_block` is on it. So the accept list IS
the right lever for the other two — my previous note said otherwise and was
wrong.

**Emptiness is detected by SHAPE and exempted by NAME.** That is the real
defect. A correct zero pays the penalty forever on any method nobody listed, and
`eth_getUncleCountByBlockNumber` answers "0x0" on every post-merge block. erpc
is filing an upstream issue for a shape-based accept, using our latency table.

Ruled out, so nobody re-treads it: the `upstreamPreForward_stateBoundary` hook
(its method class excludes ours), the accept/mark-empty defaults, the retry
executors and the error normalizer — all unchanged across the binary jump. The
open candidate remains block-availability semantics in that jump.

## 2026-09-16 What ended the 502: bracketed to erpc's binary roll

Measured, not taken from a deploy log. The shim logs ONLY when the upstream
fails, so an empty block the scraper crossed with no shim line is one the
gateway answered by itself. A scan of blocks 25,948,000-25,990,000 for a zero
transaction count finds exactly four empty blocks; chunk mtimes date the
scraper's position. All UTC — the indexer's `ls` prints UTC+2, which is a trap.

  25,948,788  synthesized 01:05:36Z, 01:05:54Z  chunk 01:08:40Z  FAILED
  25,952,297  synthesized 01:11:31Z, 01:11:49Z  chunk 01:12:10Z  FAILED
  25,966,624  synthesized 01:35:50Z             chunk 01:39:17Z  FAILED
  25,971,025  no shim line                      chunk 01:46:04Z  ANSWERED

So the fix landed between 01:35:50Z and 01:46:04Z. erpc rolled new eRPC
BINARIES at 01:41, 01:43 and 01:44Z (f9ca5de0 -> 3ec22bf8, crossing their 0.2.0
rebase; hooks.go lost ~100 lines including upstreamPreForward_stateBoundary).
Every config deploy on both sides falls outside the bracket: erpc's at 01:30,
01:33, 01:35 and 01:53Z, valve's at ~01:54Z. The chunk time is an upper bound,
so the true success may be earlier, but 01:35:50Z is a hard floor.

MECHANISM CORRECTION from erpc, which changes the fix: `markEmptyAsError`
BYPASSES `emptyResultAccept`. `trace_block` IS in
`DefaultMarkEmptyAsErrorMethods()`, so its empty becomes ErrEndpointMissingData
before the retry policy reads the accept list — adding it to the accept list
would be a no-op. `eth_getBlockReceipts` and `eth_getBlockTransactionCountByNumber`
are deliberately NOT in that list, so what produced THEIR 502s is still
unexplained. valve is holding the change until the mechanism is confirmed from
the source at the commit the fleet runs.

The penalty follows the RESULT, not the method: an unaccepted empty costs a
fixed ~645ms whatever the call (uncle count on a full block, a null receipt, an
unknown block hash), against ~110ms for an accepted one. Every post-merge block
has zero uncles, so `eth_getUncleCountByBlockNumber` pays it on every call.

THE REAL ANSWER, for later: erpc's fork has an integrity module that verifies
content instead of voting. `traceBlockGasReconciliation` checks
`len(traces) == len(block txs)`, which IS the April corruption written as a
check, and `receiptsRootRecompute` compares returned receipts against the
header's receiptsRoot. The shim approximates the first by hand. Retire the shim
in favour of those checks once they run for chain 1 in production, not before.

Probes kept, all secret-free (they run inside a container so Caddy injects the
key): `valve-tech/probes/empty-block-502.py`, `empty-result-latency.py`,
`unaccepted-empty-sweep.py`.

## 2026-09-16 The gateway's empty-block 502 stopped, cause unconfirmed

The fault ended between 01:35:50Z and 03:01Z. I did not touch the gateway. The
valve session had an eRPC config deploy in flight, which is the likely cause,
but nobody has confirmed the accept-list change was in it. If it was not, the
fault can return.

Empty block 25948788 through rpc-proxy, three consecutive runs at 03:01Z:
count 200 in 0.61s ("0x0"), receipts 200 in 0.61s ([]), trace_block 200 in
0.61s ([]). Earlier the same calls were 502 at 9.1s, 18.1s, 18.1s. A block that
does not exist now returns null and -32014, not a 502. Caddy logged zero 502s
in the hour after.

The shim logs every time the upstream fails, so it dates the fault precisely:
the last five synthesised answers were 01:05:36Z, 01:05:54Z, 01:11:31Z,
01:11:49Z and 01:35:50Z. Nothing since.

The probe is kept at `valve-tech/probes/empty-block-502.py` (also
`/root/empty-block-502.py` on the indexer). Run it from a container on
docker_default so Caddy injects the key and nothing prints a secret.

The empty block still costs 0.61s where a full block's first call costs 0.1s,
and a latency test settles what that means: the three methods are NOT on the
accept list. An accepted empty returns on the first attempt (eth_getLogs -> []
is 72ms, eth_getCode -> 0x is 72ms). All three empty-block calls land within
3ms of each other at ~608ms whatever the method, and trace_block on an empty
block (609ms) is FASTER than on a full one (988ms) — a fixed penalty, not work.
~608ms is one 500ms retry delay plus two attempts, which matches
NETWORK_FAILSAFE_BASE.

So the sweep still happens and its ENDING changed: it now returns the empty
result instead of raising the error that surfaced as a 502. Whatever did that
is in valve's commit f13ad16 and is not yet identified. The owner has ruled
that all three go on the accept list anyway, which also removes the 536ms
penalty.

Ethereum is at head: scraped through 25,987,207, no scraper errors.

The gateway work is with the valve session and the eRPC question with the erpc
session. `valve-empty-block-shim` stays whatever they ship, because it is the
only layer that checks the block header before believing an empty answer.

## 2026-09-16 The empty-block fault is the gateway, not chifra

Measured, not inferred. Block 25,948,788 holds no transactions:
- A public mainnet node answers `eth_getBlockTransactionCountByNumber` for it
  with `0x0` in 0.1s.
- Through `rpc-proxy` -> `one.valve.city` the same call returns HTTP 502 after
  9s, and `eth_getBlockReceipts` and `trace_block` after 18s. The response
  carries `Server: cloudflare` and `Via: 1.1 Caddy`, so Cloudflare fails it.
  Caddy's own log says "aborting with incomplete response, upstream
  one.valve.city".
- The header call for the same block answers in 0.1s, which is why the shim can
  prove a block empty.
- chifra has no setting for this. `chifra scrape` takes block_cnt, sleep, touch,
  run_count, dry_run and verbose. `[chains.mainnet.scrape]` holds appsPerChunk,
  snapToGrid, firstSnap, unripeDist, `allowMissing = false` and channelCount.
  `allowMissing` is about chains whose node lacks data, not about a 502.

Hand-computed hex cost an hour AGAIN here: `0x18befd4` is not 25,948,788 and
the first probe tested the wrong block. Always take the hex from `hex(n)`.

## 2026-09-16 Repair verified end to end

- `0x1c2f…d530` now returns 3 rows from the API (blocks 23076665, 23076683,
  23076692), which matches Etherscan. The page renders all 3 in a browser.
- Every mainnet address cache was spliced: 254,664,063 records -> 306,424,369.
  USDC alone gained 48.3M appearances, and Uniswap 0.78M.
- The chifra daemon was stopped during the splice, because it rewrites a
  monitor when it serves a query.
- TRAP: monitor records are NOT strictly sorted. 19 of the 34 monitors hold
  out-of-order rows (0xfe2e… has 1,395). A bisect-based patch silently dropped
  3 rows before I caught it. Select the window by value.
- The scraper is catching up at ~120 blocks/min through the shim, which has
  synthesized 4 empty blocks and refused nothing. About 5 hours to reach head.
- Still on disk, as a rollback path: `unchained/mainnet/_damaged_20260916/`,
  `cache/mainnet/monitors_damaged_20260916/`, `manifest.json.damaged-20260916`,
  the scratch index under `rescrape/`, and `rescrape/monitor-patch/*.rng`.

## 2026-09-16 Swap made; shim now serves the live scraper

- 864 damaged chunks and blooms moved to
  `unchained/mainnet/_damaged_20260916/`, and 1,438 repaired ones hardlinked in.
  The chunk ranges are contiguous with no gap, ending at block 25,948,015.
- `manifest.json` spliced: 7,624 -> 8,198 entries, matching the file count. The
  old one is `manifest.json.damaged-20260916`. The new entries carry empty IPFS
  hashes, which the weekly `chifra chunks manifest --pin` fills in.
- The shim is now a real service: `/valve/docker/empty-block-shim.py` plus an
  `empty-block-shim` service in the compose file. `scraper-eth` points at it.
  Compose backup: `docker-compose.yml.bak-20260916`.
- The scraper passed block 25,948,788 at once and is catching up, ~340
  blocks/min from 36,000 behind. NOTE: the wrapper's first run panics if the
  shim is not listening yet, then the 60s backoff loop recovers it.
- Address caches (`cache/mainnet/monitors`) are NOT yet patched, so a repaired
  address still reads as before. The sidecar answers from the cache, not from
  the chunks: for 0x1c2f…d530 it returned `monitorRecords: 1` with a coverage
  gap of 2,871,332 blocks.
- Backups: `cache/mainnet/monitors_damaged_20260916` (hardlinks, 3.3 GB).

## 2026-09-15 Mainnet re-scrape done; swap not yet made

- The watcher reads DONE (2026-09-13 16:12 UTC). All 1,438 scratch chunks in
  23,003,809–25,000,000 passed the per-block txid check. It logged zero BAD lines.
- Block 25,000,000 ends a chunk in both indexes, so the swap boundary is clean.
  Production holds 864 chunks in that range; scratch holds 1,438 (block_cnt 2000).
  Scratch also holds two chunks past 25,000,000. Do not copy them.
- Proof on the reported address: a direct chunk read finds 1 appearance for
  0x1c2f…d530 in production and 3 in scratch (23076665, 23076683, 23076692).
  Etherscan shows 3.
- `unchained/mainnet/manifest.json` lists the old chunk hashes. Rebuild or
  splice it after the swap.
- There are 35 mainnet monitors (3.3 GB; USDC alone is 1.5 GB). Do NOT reset
  lastScanned and let chifra rescan: chifra loads a whole monitor into memory
  (see the indexer memory note). Instead, rebuild each monitor's records for
  the range from the scratch chunks and splice them in.
- The live `valve-scraper-eth` is still stuck, now 36k blocks behind head. It
  retries from 25,948,759 and fails on block 25,948,788, which is empty (the
  empty-trie root). This confirms the gateway empty-block 502 theory.

## 2026-09-10 Bug #3: token balances show nothing — investigation

The frontend logic is correct: `resolveTokensSection` tries the indexed
holdings gateway first, falls back to the RPC token list. Both paths are
wired and the `TokensTab` renders whichever returns.

The bug is backend/runtime:
- The holdings gateway returns `indexed: false` when the chain's substreams
  sink table doesn't exist yet, and empty holdings in that case.
- The RPC `getAddressTokens` discovers tokens by scanning Transfer events in
  the address's recent `TOKEN_SCAN_APPEARANCES` (50) appearances. An address
  that hasn't touched a token recently returns empty.

When both return empty, the section shows "No tokens found" — truthful but
unhelpful. This needs a running server + live RPC to diagnose further.

## 2026-09-10 Bug #9: method column + EIP-1559 badge

Added a Method column to TxTable (address view transactions tab). Shows the
function name via `MethodName` (with candidate-count tooltip), or a Transfer
pill when there is no function name. The BlockView already had this column;
the address view did not.

Fixed TxGasInfo: EIP-1559 was de-emphasized (plain text, no badge) while
every other tx type got the `theme-tertiary-bg` badge. EIP-1559 is the
common type — rendering it without a badge made it read as "no type."
Removed the `isDefaultType` special case so every type gets the same badge.

## 2026-09-10 ResolvedAmount wired into amount call sites

Wired `toResolutionGroups` into every dense-table amount cell via a new
`ResolvedAmount` primitive (`packages/web/src/components/primitives/ResolvedAmount.tsx`).
It trims a `formatPLS`/`formatAmountDisplay` string to 3 significant groups and
wraps it in `CopyableValue` (hover shows the full formatted value, click copies).
When nothing was trimmed, it renders a plain span — no noise tooltip.

Call sites wired:
- TxTable value column (`AddressView/TxTable.tsx`)
- BlockView tx value column (`BlockView.tsx`)
- TxList right column (`ExplorerHome/TxList.tsx`) — `formatNative` + symbol
- TokenTransfersSection amount column (`TxDetail/TokenTransfersSection.tsx`)
- TokensTab balance column (`AddressView/TokensTab.tsx`)
- PortfolioPanel holdings total + native balance list (`workspace/PortfolioPanel.tsx`)
- FeeLadder tx value (`networkHealth/FeeLadder.tsx`)

NOT wired (detail views, full precision is correct):
- AddressHeader balance (`AddressView/AddressHeader.tsx`) — the page header
- OverviewSection value (`TxDetail/OverviewSection.tsx`) — the tx detail page

The `explorerAddressView` test was updated: the indexed-holdings balance
"5,456.5076" is now visible as "5,456.507" (3 groups), with the full value in
the CopyableValue aria-label. The non-indexed fixture "5,456.51" (2 integer +
2 fraction digits) is unchanged — 2+1 ≤ 3, so nothing is trimmed.

## 2026-09-10 CopyableValue + MiddleTruncate wiring + resolution utility

Landed the two handoff pieces from `trace-handoff/HANDOFF.md`:

1. `packages/web/src/lib/format/resolution.ts` — three-group resolution: trims
   a grouped decimal string to at most 3 significant three-digit groups,
   counting from the most significant one. String-only (no `Number()`), so
   values past 2^53 stay exact. 8 tests pass.

2. `packages/web/src/components/primitives/CopyableValue.tsx` — hover shows the
   full value in a themed `Tooltip`; click copies it. A `<button>`, not a span
   with onClick (keyboard-accessible). `stopPropagation` + `preventDefault` are
   load-bearing: these sit inside rows/cards that are themselves links.

Wired `MiddleTruncate` through `CopyableValue` (the highest-value wiring step
per the handoff). New `copyable` prop (default `true`):

- `copyable={true}` (default, desktop): the clipped value is wrapped in
  `CopyableValue` — hover shows the full value in a themed `Tooltip`, click
  copies it. Replaces the native `title=` tooltip (waits ~1s, unthemeable,
  invisible to keyboard users). The full value stays in the DOM text either way.
- `copyable={false}`: the `title=` attr stays for contexts where `MiddleTruncate`
  sits inside an `<a>` or `<button>` that handles navigation (ExplorerLink,
  EntityRow, MinersPanel's `<Link>`, TokensTab's button). `CopyableValue`'s
  button does `stopPropagation` + `preventDefault`, which would block
  navigation; the `title` still shows the full value on hover.

Call sites updated: AddressCell, TxTable (3), BlockView (4), TxList, MempoolView
(2), TrackedTxPanel, MinersPanel, TokensTab — all link-wrapped, `copyable={false}`.
GasTable, DecodedInputSection, PortfolioPanel (2) — standalone, default `copyable`.

The `title` prop was removed from `MiddleTruncateProps` — no call site used it.

Tests updated: MiddleTruncate (11, rewritten for the two paths), gasProfiler,
explorerMopUp, explorerTxDetailSections, PortfolioPanel — all 95 tests in the 10
touched files pass. Typecheck, lint, build clean. The 295 pre-existing jsdom
failures (localStorage/fetch) are unchanged on `main`.

Next: wire `resolution.ts` into amount call sites (handoff item #2), then the
real bugs (#3 token balances, #9 tx tab call signatures).

## 2026-09-10 THE MAINNET INDEX IS MISSING TRANSACTIONS FOR 1.94M BLOCKS

Reported: 0x1c2f39e7…d530 shows 1 transaction here and 3 on Etherscan. The
API, the sidecar and chifra all agree on 1 row. **The index itself is wrong.**

**Damaged range: mainnet blocks 23,003,809 → 24,945,991** (823 finalized
chunks, roughly late July 2025 → late April 2026). In every sampled block there,
the chunk holds only 23–77% of the block's transactions. For example, block
23076665 has 236 transactions and the chunk records 124. Every sampled chunk
outside that range is complete. PulseChain and PulseChain Testnet v4 are
complete in every sample, including their own bulk passes on the same days.

How it was measured: parse the chunk (44-byte header, 28-byte address
records, 8-byte appearances), count distinct txids per block (skip >= 90000,
the reward sentinels), and compare with `/api/block/N` transaction counts.

**The extractor is not at fault.** `chifra blocks --uniq 23076665` today
returns all 236 txids, including the reported address. So the chunks were
written from incomplete data at scrape time.

**The damage matches a write date exactly.** Blocks 0–23,003,808 were written
2026-04-22 (the initial bulk load) and are clean. 23,003,809–24,943,854 were
written 2026-04-24 in one catch-up pass. That pass damaged every sample, and
the first chunk written on 04-25 is damaged too. Chunks from there onward are
clean, including the catch-up bursts on 08-08, 08-30 and 09-04. The scraper
container was recreated on 2026-08-08, so no April logs survive. The
rpc-proxy's comment about 502s "mid-batch" under HTTP/2 is the best candidate
for the mechanism, but it is NOT proven.

**Every mainnet monitor built from that range is incomplete too.** A monitor
caches what the chunks said. Re-scraping the chunks does not fix a monitor
whose header already says "scanned to N". The monitors must be rebuilt.

**Separate, live:** the mainnet scraper has been failing on
`502 Bad Gateway` at block 25,948,788 since 20:29 today. It loops every ~45s
and the index sits ~700 blocks behind head. It stalled the same way at
25,931,228 (1,257 errors) earlier this week.

### The mechanism is proven, and it is still live

`one.valve.city` sometimes answers `trace_block` with `{"result":[]}` — 200,
36 bytes — for a block with hundreds of transactions. chifra cannot tell that
from "no traces", so it indexes only the receipt log addresses. Block
23076665: the chunk dropped 114 txs, and all 112 log-less txs are among them;
no log-less tx survived. For other blocks in the range the gateway returns a
Cloudflare 502 after 18s — even for `eth_getBlockTransactionCountByNumber`.
Today it answered block 23005399 once with `[]` and then 502 five times running.

### Repair: started, then stopped on purpose

Scratch index at `valve_tb_data/_data/rescrape/unchained/mainnet`: the 5,952
clean chunks and blooms are HARDLINKED there (≤ 23,003,808), plus a copied
`manifest.json` and its own `ts.bin`. `seed-baseline.txt` beside it holds
inode/size/mtime for every linked file, to prove later that nothing rewrote a
production chunk. Container `valve-rescrape-eth` is STOPPED — it scrapes
through the same `rpc-proxy` → `one.valve.city` path, so it would write the
same damage again. `TB_SETTINGS_INDEXPATH` / `TB_SETTINGS_CACHEPATH` redirect
chifra cleanly; verified before starting.

chifra discards a whole 2,000-block pass on any 502, then restarts it. That is
why the live scraper sits on one block for hours.

Blocked on a trace source. The indexer's own reth is Sepolia. The direct
mainnet nodes (`direct-{a,b}-evm-1-rpc.valve.city`) answer 403 without
credentials. Once a source is chosen: point the scratch container at it, scrape
past 25,000,000 (a snap-to-grid boundary, so the swap lines up), verify EVERY
block's txid count against `eth_getBlockTransactionCountByNumber`, swap chunks
and blooms (damaged ones kept as a backup), then patch each mainnet monitor to
`lastScanned = 23,003,808` with its records above that dropped, so it rescans.

### Repair restarted 2026-09-11 03:32 UTC, behind a per-block gate

The owner approved the unlimited valve key (`vk_Et-…`). Probing every route
with it: `one.valve.city/rpc/<key>/evm/1` and `rpc.valve.city/v1/<key>/evm/1`
work; `rpc.valve.city/rpc/…` and `evm-1-rpc.valve.city` say "API key
required"; the direct nodes stay 403 even with the key as a bearer.

**The gateway flaps in time windows.** Within minutes, the same `trace_block`
went 200 → 502 on every route and client → 200 again. It is not the client:
curl, Python urllib and Python keep-alive all got 151,611 bytes back to back
once the window passed. Request shape (compact vs spaced JSON) made no
difference either.

So the scratch scraper stays on `rpc-proxy` (the key never reaches a chifra
log line — chifra prints its RPC URL at startup), with `--block_cnt 500` so a
502 wastes 500 blocks, not 2,000. Unverified ripe/staging files from the flaky
window were deleted first, after proving the seeded chunks unchanged against
`seed-baseline.txt`.

The gate is `~/rescrape-verify.py` on the indexer: every block in every new
chunk must have exactly as many distinct txids as
`eth_getBlockTransactionCountByNumber` says. Passing chunks append to
`rescrape/verified.txt`, so it resumes. A local watcher runs it every 10
minutes and stops `valve-rescrape-eth` on the first BAD chunk. A bad chunk
means: delete it and every chunk after it in scratch, then resume.

### The gateway cannot answer EMPTY blocks — this stalls every scraper

RETRACTED: an earlier checkpoint here blamed an "~18s deadline and warm-up".
That came from curl probes where I hand-computed the hex block numbers WRONG
(`0x15f07cd` is 23,005,133, not 23,005,005). Always build the param with
`hex(b)` or `printf 0x%x`, never by hand.

The real finding, measured with correct numbers: 23005005, 23005399 and
25948788 are EMPTY blocks — 0 txs, gasUsed 0, transactionsRoot = the empty
trie root; Etherscan agrees. For each, `eth_getBlockByNumber` works but
`eth_getBlockTransactionCountByNumber` 502s at 9s and `eth_getBlockReceipts` /
`trace_block` 502 at 18s — on every route, every client, every retry. Inside
a batch the same items come back as `{"code":-32603,"message":"RPC proxy
unavailable"}` under an HTTP 200. The gateway treats an empty answer as a
failure. 5 of the 5,000 blocks after 23,004,809 are affected, so the damaged
range holds on the order of 2,000. Each one stalls chifra forever: that is
why the live scraper has sat on 25948788 since 2026-09-10 20:29.

The fix belongs to the valve gateway. Until then the re-scrape goes through
`valve-rescrape-shim` (python:3.12-alpine on docker_default, source at
`~/rescrape-shim.py` on the indexer). It forwards to `rpc-proxy`; when an item
fails, it answers 0 / [] ONLY if the block header proves the block empty. It
also turns an empty trace/receipt list for a NON-empty block into an error,
which blocks the April failure mode at scrape time. The gate reads through the
same shim.

Also: scratch `ts.bin` was a fresh IPFS download ending at 23,002,097, so
chifra backfilled timestamps one block at a time (~11/s) on every pass. It is
now a COPY (not a link — the scraper appends) of production's, 25,948,787
records. Backfill lines dropped to zero.

### Where the re-scrape stands (2026-09-11 04:20 UTC)

Running through the shim with `--block_cnt 2000`: zero scraper errors, the
shim answered 21 empty-block calls and refused none. The first three new
chunks (23,003,809–23,009,751) pass the per-block gate. They are denser than
the damaged ones, as complete data should be: the stretch the old 2,888-block
chunk covered now needs two chunks, of 1,741 and 2,116 blocks.

Rate ~590 blocks/min, the same at 500- and 2,000-block passes. About 1.99M
blocks remain, so expect verification past 25,000,000 around
2026-09-13 12:00 UTC.

The gate watcher now lives ON THE INDEXER, because the laptop killed two local
background jobs under memory pressure: `~/rescrape-watch.sh`, started with
`nohup setsid`, log in `~/rescrape-watch.log`, one status line in
`~/rescrape-watch.status` (RUNNING / BAD / DONE / STOPPED). It stops
`valve-rescrape-eth` on the first BAD chunk, and again once verification
passes 25,000,000. After a reboot, restart it by hand; nothing else does.

### OPEN

- Finish the repair: when the status reads DONE, swap chunks 23,003,809 →
  25,000,000 (keep the damaged set as a backup), patch the mainnet monitors,
  then remove the shim and scratch containers.
- The live scraper is still stuck on empty block 25948788. Routing it through
  the shim would unstick it, but that changes production — ask first.
- Gateway: raise or bypass the ~18s per-attempt deadline for `trace_block`.
- The gateway itself: empty `trace_block` results and 502s on mainnet blocks
  23.0M–24.9M. This affects every customer of `one.valve.city`, not just us.
- The indexer RPC key was printed to a session transcript while reading
  `rpc-proxy.Caddyfile`. It needs rotating.

### Shipped the same day

- The breadcrumb shows the chain icon before the entity on every single-chain
  view (the chain it READS: scoped, or DEFAULT_CHAIN_ID when bare).
- One time vocabulary in `lib/format/time.ts`: "1y 1mo", calendar months in
  UTC, `formatSince` for app activity in ms. `<Timestamp>` follows a new
  Settings → Time choice (relative / UTC date / both); detail pages always
  show both. Ten duplicated helpers deleted.

## 2026-08-27 A CALL TREE, A PAGE SIZE, AND TWO AGENTS IN ONE TREE

### Another agent lost two files in this working tree. Read this first.

A second agent was working in `github/trace` at the same time as me. It had
two finished, UNCOMMITTED files on disk:

  packages/web/src/lib/format/resolution.ts       (toResolutionGroups, 8 tests)
  packages/web/src/components/primitives/CopyableValue.tsx

I found them untracked, deliberately left them out of my commits, and said so.
They disappeared from disk at 22:02 while I was working. I did not delete them,
and I cannot prove who did. They were never staged, so **git holds no object for
them** — `git fsck`, the reflog, the Vite cache and the dist bundle all come up
empty. The only surviving copy is that agent's own context.

**One agent per repository at a time.** Disjoint files are not enough: the git
index, the dev server ports and the working tree are all shared. Port 10100 was
already taken when I started `npm run dev`, which is the same collision showing
up as an EADDRINUSE.

### Internal transactions render as a call tree

The section was five columns — Type, From, To, Value, Gas — that never said
what was being called. Four collapse into one expression per row:

```
CALL         WPLS.approve(0x98bf…cc02, 3,691,074,…) → true    24,399 gas
CALL         0x98bf…cc02.swapExactTokensForTokens(…, +3)     101,362 gas
├─STATICCALL 0x3474…c9ad.getReserves() → 445,033,858,563, …    2,504 gas
└─CALL       0x3474…c9ad.swap(1,776,662,498, 0, +2)           77,039 gas
```

**The tree was always in the trace; the wire shape threw it away.**
`flattenInternalCalls` walked a `CallFrame` tree and emitted a flat list. Each
row now carries the `depth` it sat at. Depth-first order plus a depth is
lossless — a row's parent is the nearest earlier row with a smaller depth — and
the list stays flat, so no existing consumer broke.

**Never infer the nesting from `from` chaining.** A DELEGATECALL keeps the
caller's address, so parent and child report the same `from`.

**A return type is not derivable from 4byte.** It stores argument types only.
`decodeReturn` uses a small table of signatures a standard fixes (ERC-20/721,
the V2 pair) and renders nothing for anything else. `transfer(...) => true` is
a fact; a guess would not be.

**Measured in the browser, not just in tests.** 21 calls, three depths, both
swap subtrees drawing identical guides. The first pass drew a guide at depth 1,
where a direct call renders no connector — two depth-3 rows then looked
differently indented, and only `getBoundingClientRect` on the guide spans
showed it. At 375px the tree scrolls in its own box; the page body does not.

**IndexedDB served the pre-change response** and the first render showed raw
selectors with no names. The API was already correct. Clear `keyval-store`
before believing a stale-looking page.

### The address page never let you ask for more rows

There is no 250 limit. `routes/explorer.ts` clamps `limit` to **100**, and it
clamps SILENTLY — `?limit=250` answers 200 with 100 rows. The 250 people
remember is chifra's daemon `maxRecords`.

Measured against production: 25 rows in 1.3s, 100 rows in 1.6–2.5s. Each row
costs three RPC reads that viem batches.

The footer now says "Page 1 of 3,882" instead of "Page 1", which is what made
a six-hour first page read as the whole history of a 97,027-appearance
contract. Changing the size keeps your place: page 41 of 25 starts at row 1000,
so it becomes page 11 of 100.

The size lived as two constants, one in the hook and one in the tab. Changing
either broke the other's "has more" test.

### OPEN

- **Token amounts render at full wei scale**, up to 27 digits, in the call
  tree. The three-group resolution formatter was the fix — and it is one of the
  two files that disappeared.
- The API's silent clamp at 100 is still silent. The etherscan-compat handler
  passes `offset` straight through and skips the clamp entirely.

## 2026-08-27 I CAUSED AN OUTAGE, AND FOUND FOUR REAL BUGS FIXING IT

**Everything is back up.** All four chains answer, the otter pages render, and
the address page that started this returns rows.

```
                         before              after
all four chains          401                 200
otter address page       blank               25 rows
Ethereum address page    total 2, rows 0     total 2, rows 2
```

### The outage was mine

I ran `explore-deploy` off the `thatis-monitor-key` branch without reading what
its config change did. That branch had rotated `otterBackendKey` to an `op://`
ref, so `explorer-write-trace-env` composed the RPC URL from the **v2** key and
wrote it to the explorer box — before the api had seeded it. The commit message
predicted this failure in so many words, and the runbook's status line said
"changed in-repo and NOT deployed".

**Never run a deploy off a branch without reading the diff of its deploy
config.** `sync-code` rsyncs the WORKING TREE, not the index, so uncommitted
work deploys too — mine and anyone else's.

### Four bugs found while recovering

1. **`sync-code` shipped 789 MB of gitignored build output** to three hosts
   every deploy (`services/substreams-reference/target` 414 MB,
   `…/erc20-balance-changes/vendor` 375 MB). It blew the 600s timeout and
   failed the recovery deploy. rsync walks the tree, not the index.
2. **chifra logs lack four JSON-RPC fields** (`blockHash`, `blockNumber`,
   `transactionHash`, `transactionIndex`), so ethers rejected the receipt and
   ONE token transfer blanked a whole otter page. The `to: "0x0"` bug one field
   along.
3. **The test asserted the buggy pass-through.** It checked `logs` came back
   byte-identical to the input, so it pinned the defect rather than catching
   it. A test can encode a bug.
4. **The trace deploy plan had no relay preflight.** The otterscan plan has one
   and it works; the trace plan consumes the same key and had nothing. That gap
   is what let me take production down.

### Retiring a system key is not what it looks like

Removing an entry from `SYSTEM_KEYS` does NOT retire the key — it PROMOTES it.
`SYSTEM_KEY_IDS` is derived from the roster, and `008_key_config_rehydrate`
uses it as a skip set; drop the entry and the surviving `project_key` row stops
being skipped, so 008 republishes it to Redis as an ordinary key. It keeps
authenticating while the source says it is gone. Clearing Redis by hand is
undone by the next deploy. The fix is `unlinkedAt` on the row. Runbook
corrected (monorepo `dff0225`).

Found only because the api suite runs against a PERSISTENT local Postgres —
a fresh-DB suite goes green and proves nothing.

### Verification lessons, all of them mine today

- **A 200 is not evidence.** I called `valve.city/auth/cli` "a real page" on a
  200 and a byte count. It is an SPA: every path returns 200 + index.html. I
  had the counter-example in the same session and used it as proof anyway.
- **The app is a HASH router.** `/auth/cli` renders the landing page;
  `/#/auth/cli` renders the route. I had a memory saying exactly this and did
  not apply it.
- **Do not verify a fix with data you just created.** My own diagnostic
  `chifra list` created the monitor for the reported address, so the sidecar
  then answered and the bug looked fixed. Test with something untouched.
- **`explore-rollback` recreates from the CURRENT image.** After rolling back,
  `/health` still reported the new SHA. Check `version.shortSha`, not the plan
  name.
- **Piping deploy output through `sed`/`tail` buffers it.** Two runs looked
  silent for minutes. Log to a file and grep the file.

### The tokens report was a misdiagnosis

The tokens tab IS correctly chain-scoped: 369 shows 5 tokens, 943 shows only
CHIP, and they do not mix. "Dai Stablecoin from Ethereum" is the token's own
on-chain name. But `Claim 50k $pDAI on dai.free.nf` is a phishing token
rendered verbatim beside real balances — attacker-controlled text with the
authority of verified data, same class as the 4byte guess markers. Task #38.


## 2026-08-27 THE RPC KEY IS DEAD, AND IT WAS LEAKING TO THE PUBLIC

Two production problems, one of them a security incident. Both found chasing
a user report that Ethereum addresses "do not load anything even though the
count is 1".

### The key leaked in every error body (FIXED, needs rotation)

Our RPC keys live in the URL **path**, and viem puts the full request URL in
the message of every transport error. `respond.fail` passed that message
straight to the client, so any failing endpoint handed a 35-character `vk_`
key to any unauthenticated caller, on all four chains:

```
{"ok":false,"error":"HTTP request failed.\n\nStatus: 401\nURL: https://one.valve.city/rpc/vk_…/evm/1
```

`scrubInternal` only stripped viem's version footer, and the `ApiError` path
was not scrubbed at all. `lib/redact.ts` now runs at the boundary — ApiError,
the 500 path, and `console.error`, because a key in a log is still a leak. It
strips `vk_` tokens, bearer tokens and URL userinfo, then collapses any
absolute URL's path, which catches key shapes we have not named. Host, status
and reason survive so the message stays diagnosable. Verified live on all four
chains: `https://one.valve.city/***`.

**The key still needs rotating.** The fix stops the bleeding; it does not undo
the exposure.

### Every chain's RPC is 401 (NEEDS THE USER)

The same key is revoked or expired: `Invalid or inactive API key` on all four
chains, `/health` reports every chain `degraded` with `blocks=0`.

The gateway is FINE — the public `vk_demo` key returns current blocks on
`evm/1` (0x18a66af) and `evm/369` (0x1a1fe06). So this is our credential, not
the service.

That is the root cause of both user reports. On Explore, chifra supplies the
appearance COUNT (so the page says "2") while every RPC hydration call 401s
and is swallowed by `catch { return null }` — count without rows. On
Otterscan, the same dead RPC sits behind it, and the ethers
`check failed (argument="to", value="0x0")` is otterscan choking on a failed
receipt.

### I shipped a regression, and it was the session's own bug class

The appearance sidecar reports honestly whether it read the whole range —
`coverage.complete` and `totalIsExact` — because it does not parse the binary
finalized chunks. **The client ignored both fields.**

An address chifra has never been asked about has NO monitor, so the unread
range is the entire chain and the sidecar returns an empty list. The address
page rendered that as "no transactions". Mainnet holds 28 monitors, so nearly
every address on the chain read as empty. Binance 14 was worse in kind: a
plausible-looking `total: 50,291,073` that had not read the gap either.

`isCompleteAnswer` now gates both call sites — use the sidecar only when it
says it read the whole range, otherwise fall through to chifra, which reads
the chunks and is already bounded by `heavyGuard`. WPLS still reports
`complete: true`, so the fast path survives.

The lesson is the one this whole session has been about, and I still walked
into it: **a fast wrong answer is worse than a slow right one**, and a source
that tells you it is incomplete has to be believed.

### Traps

- **Do not verify a fix with an address you just queried.** My own diagnostic
  `chifra list` CREATED the monitor for the reported address, so the sidecar
  then answered correctly and the bug looked fixed. It was not. Test with an
  address you have never touched.
- **`explore-rollback` recreates the container from the CURRENT image.** After
  rolling back, `/health` still reported the new SHA — the code did not
  revert. Check `version.shortSha` rather than trusting the plan's name.
- The hook at `~/.claude/hooks/no-secret-to-terminal.py` blocks `op item get`
  pipelines. It is right to; use the key rather than printing it.


## 2026-08-26 THE HEAVY ADDRESS IS FIXED. WPLS ANSWERS IN 0.5s.

**Shipped and live.** The address page that returned HTTP 524 at 125s now
answers from production in under a second, with data current to the head
block.

```
                    before                  after
WPLS page 1         HTTP 524 at ~125s       0.54 - 0.87 s
WPLS deep page      impossible              1.66 s  (page 5,000)
WPLS total          never returned          224,844,066
newest row          stale or absent         head block
small address       1.5 s                   byte-for-byte identical
```

Three pieces, all deployed:

**1. A memory cap on the chifra daemon** (monorepo `9c7dd0c`). `mem_limit:
16g` + `memswap_limit: 16g` on `valve-trueblocks`. Chosen from the monitor
size distribution at 28x: it refuses exactly WPLS (~47 GB) and USDC (~39 GB),
the two the sidecar serves, and permits every other monitor — there is a real
cliff after those two. The kernel oom-killer picks its own victim, so an
unbounded chifra threatened reth and everything else; now the cgroup kills
chifra alone and docker restarts it.

**2. The appearance sidecar** (monorepo `2d699ac`, `5a3b1c8`). A
dependency-free ESM service on `node:22-alpine`, source bind-mounted, data
volume mounted READ-ONLY, capped at 1g, published on 127.0.0.1:8090 and
routed at `chifra.valve.city/appearances`. 69 tests, green in the container.

**3. The API prefers it** (`bda900f`). `fetchAppearances` returns `null` —
never an empty list — on any failure, so an unreachable sidecar degrades to
exactly the old chifra behaviour. A body with even ONE unreadable row is also
null, because a short page would silently lose a transaction.

### What made it possible

Not speed work. The index was never slow — chifra's read path was. Raw `dd`
reads the 1.7 GB WPLS monitor in 0.70s cold; chifra takes 44-69s for the same
bytes, 63-250x slower, because it loads the whole monitor to answer anything.

And the freshness came free. The scrapers write one TSV per block under
`unchained/<chain>/unripe/`, so the newest ~1,900 blocks are a millisecond
grep. A monitor is only a derived cache. That is why the sidecar can be both
instant AND current to the block, which chifra could not be at any size.

### The gap, and why every response admits it

Finalized chunks are binary and the sidecar does not parse them, so a monitor
lagging the finalized frontier leaves a range readable by nothing here.
Measured across the live monitors it runs from -1,100 blocks (the monitor is
AHEAD, so the tiers overlap it) to **539,948 blocks — about 62 days**.

Every response carries `coverage.gap` and `totalIsExact`, and `total` is a
stated lower bound when a gap exists. Verified live: the 62-day-stale monitor
returns `totalIsExact: false` with the gap named, while still serving
head-current rows.

The two sources are reconciled by BLOCK NUMBER, not deduplication. A monitor
consolidated through block L holds everything at or below L, so tier rows at
or below L are dropped and what remains above L is exactly what the monitor
lacks. Exact, and it keeps paging as plain arithmetic.

### Traps worth keeping

- **`chifra chunks index --belongs` is not a gap-closer.** It returns an
  EMPTY result for a known-positive control (WPLS in a chunk it certainly
  appears in). Without that control I would have recorded "the gap is empty".
  Re-run the control before trusting it again.
- **A client timeout does not cancel chifra's work.** A killed request kept
  writing for hours. Our 30s abort stops us waiting, not the box working, so
  retries stack instead of replacing each other.
- **The API envelope is `{ok, result: {transactions}}`.** Parsing it as
  `{transactions}` yields a confident "0 rows" against a perfectly healthy
  endpoint. Cost me a false alarm here, and the same shape mistake earlier.
- Deep paging is a seek: offset 5,000,000 costs 0.175s on the box.

### Open

Parsing the finalized chunk format would close the gap for good. Until then
khedra's monitor service (`enabled: false`, "no watchlist yet") is the warmer
that keeps gaps near zero — but note it can no longer freshen WPLS or USDC
through chifra, because the 16g cap refuses them by design.


## 2026-08-26 THE INDEX IS PER-BLOCK CURRENT. CHIFRA'S READ PATH IS THE BUG.

**Measured directly on the indexer box (95.217.41.159:2222), read-only.**
This overturns the 2026-08-25 conclusion below it. That entry is right about
where the time goes and wrong about why.

**Chifra is 63-250x slower than the disk on the same bytes.** The WPLS
monitor is 1,798,357,424 bytes. Raw `dd`: **0.70s cold, 0.20s warm**
(2,449 / 8,736 MB/s). Through chifra: **44-69s** (~37 MB/s). So the 37 MB/s
was never I/O. It is chifra's per-record loop over 224.7M records. The data
was never the problem.

Three consecutive WPLS reads cost 69s / 55s / 54s with the file barely
growing. With nothing to freshen it still cost 54s, so **that cost is the
return path, not the freshen** — chifra hands back 224.7M appearances to
give you 25.

**`--count` is worse than a full list.** `chifra list --count` on WPLS ran
300s and was killed by the client timeout, returning nothing. The count is
`(filesize - 8) / 8`; `stat` answers it instantly.

**`firstBlock`/`lastBlock` do not bound the work.** They filter output. A
100-block window on WPLS does not return quickly, while the same window on a
small monitor takes ~1s. Same finding as `maxRecords`.

### The file format, confirmed

`.mon.bin` is a flat array: 8-byte header, then 8-byte records
(`blockNumber` uint32, `transactionIndex` uint32, little-endian), ascending.
`(1798357424 - 8) / 8 = 224,794,677`, exactly the appearance count. A
seek-read of the count plus the last 25 records takes **0.027s**.

### Four tiers, and the monitor is only a cache

The index is NOT the monitor. TrueBlocks keeps four tiers under
`unchained/<chain>/`, and coverage is contiguous:

| tier | covers | behind head |
|---|---|---|
| `finalized/` | -> 27,382,782 | ~1,090 blocks |
| `staging/` (one 23MB TSV) | 27,382,783 - 27,383,843 | ~28 blocks |
| `unripe/` (one TSV per block) | 27,383,844 - 27,383,871 | **0** |

Both TSV tiers are plain `address<TAB>block<TAB>txIndex`. The scrapers write
`unripe/` **every block**. Grepping all 28 unripe files for WPLS: **0.002s**.
Grepping the whole staging tier (14,940 hits): **0.012s**.

So per-block accuracy needs no monitor refresh at all. It is a 14ms grep of
text files that are already on disk. `monitors/*.mon.bin` is a derived cache
on top of these tiers, nothing more.

**Monitors do not advance on their own** — only a `chifra list` call
consolidates one. That fact stands. The conclusion drawn from it on
2026-08-25 (that a seek read would serve stale data) does not: the freshness
lives in the TSV tiers, not the monitor. Right now the WPLS monitor's last
record is block 27,381,192 against head 27,383,871 — **2,679 blocks, roughly
seven to eight hours stale** — because nothing has called it since the last
test.

### Reading a heavy monitor OOMs the daemon. This is reachable from production.

At 13:51:09 the kernel OOM-killed `chifra daemon` on the indexer:

```
Out of memory: Killed process 2915960 (chifra)
total-vm:95984264kB, anon-rss:52972520kB
```

**50.5 GB resident, 96 GB virtual, on a 62 GB box.** I triggered it with a
handful of concurrent WPLS probes. Docker restarted the container 15s later
and it came back healthy; the four scraper containers, the index and every
other monitor were untouched, and the index never stopped advancing.

This is not just my accident. `RestartCount` is 4, and the container had
already restarted ~9h before I touched it while its siblings sat at 2 days.
The mechanism is reachable from the product: **a user loading a heavy address
page can OOM the indexer's daemon**, and because a client timeout does not
cancel the work (below), retries stack instead of replacing each other.

Order of magnitude: WPLS is 224.7M appearances in a 1.8 GB file, and chifra
needed ~50 GB of RAM to read it. That is ~28x the file size.

The killed process left a 1.8 GB `monitors/staging/` file behind. It was
record-aligned but written by a dead process, and the next freshen would have
consolidated it into a 224M-record monitor, so it is moved aside as
`…mon.bin.orphaned-oom-20260826` rather than trusted. Staging is regenerable
from the index; the other 72 monitors were untouched.

### A client timeout does not cancel chifra's work

A killed `--count` request left `chifra daemon` (pid 2915960) writing
`monitors/staging/0xa1077a…mon.bin` for hours afterwards, past 1.4 GB and
still growing, holding the fd open. **Our 30s API timeout does not stop the
daemon**; it only stops us waiting. This is a live source of load on the box
and it is worth handling before adding any warmer that can queue more of it.

### khedra already has the warmer, switched off

`/valve/docker/khedra.yaml`: `services.monitor.enabled: false`, commented
"no watchlist yet. Disable until we have a list of addresses to track."
`sleep: 12`, `batchSize: 500`. Its control API on :8338 reports both
`scraper` and `monitor` as `paused`. Scraping is done instead by four
per-chain scraper containers. So task #34 is a watchlist plus a config flag,
not new software — but note that one WPLS freshen is expensive enough to
need thought before it goes in a 12-second loop.

### What the read path should be

Newest-first, which is what the address page shows first:

1. **`unripe` + `staging` grep** — last ~1,090 blocks, ~14ms, current to the
   block. Serves the first page of almost any address.
2. **monitor seek** — deeper pages, ~27ms, no scan.
3. **`finalized` chunk reads** — only when the monitor has fallen behind.

The warmer's job is therefore NOT freshness. Freshness is free from tier 1.
The warmer exists to keep the monitor caught up to the finalized frontier so
tier 3 stays empty and we never have to decode bloom-filtered chunks
ourselves. That gap is currently 1,590 blocks and grows without bound.

Tiers 1 and 2 can overlap after a warm, so the merge needs deduplication —
pin it with a test rather than assuming the ranges stay disjoint.

Box facts: 12 cores, 62 GB RAM, load ~8.5, 49G disk at 39%. Monitor volume:
pulsechain 73 monitors / 5.1G, mainnet 28 / 3.3G, pulsechain-v4 25 / 81M,
sepolia 2 / 92K. Volume `valve_tb_data` -> `/var/lib/docker/volumes/valve_tb_data/_data`.
No `/usr/bin/time`, no `bc` on the box. `chifra monitors` has no `--freshen`
in this build.


## 2026-08-25 A HEAVY ADDRESS: WHERE THE 30 SECONDS ACTUALLY GO

**Hydration is not the bottleneck. The appearance index is all of it.**
Measured against the live API and chifra.valve.city, chain 369.

Hydration, isolated by hitting the same page twice inside the 30s appearance
cache (so the second call is pure RPC): **25 rows in 0.96s, 100 rows in
2.58s** — about **25 ms per row**, batched and bounded. It never needed
fixing.

The index read is the whole cost. First read of the day, `list --reversed
--max_records 25`:

```
        52 appearances      1.8s      5.3M appearances     2.7s
    92,572 appearances      1.7s     30.6M appearances   125.5s -> 524
   526,139 appearances      1.9s     43.7M appearances   125.2s -> 524
                                    224.7M appearances   125.6s -> 524  (WPLS)
```

**Size is a proxy. The real driver is the FRESHEN BACKLOG.** `chifra list`
brings the address's monitor file up to head before it pages it, and that
catch-up is the bill. Two addresses of the same size prove it: 0x95b303…
(23.9M appearances) needed four attempts, while 0x28c6c0… (22.55M) answered
cold in 10.8s. The direct measurement: on 0x0cfd4b… the monitor grew
38,204,576 -> 42,286,848 bytes (~510k new appearances) during a 26.5s call,
and the very next read took 2.7s. Big monitors correlate with big backlogs
only because a busy address accrues more appearances per block.

**Four facts worth never re-deriving:**

1. **`chifra list --count` is NOT the cheap half**, whatever the old comment
   in `appearances.ts` said. It freshens the same monitor and costs the same
   order — 524 at ~125s on every monitor above 30M appearances.
2. **`firstRecord`/`maxRecords` are already used and do not rescue a big
   address.** chifra freshens before it pages, and the OpenAPI says
   `firstBlock`/`lastBlock` are "ignored when freshening". A block-bounded
   read of WPLS measured 125.5s — identical to the unbounded one.
3. **chifra.valve.city sits behind Cloudflare, which cuts the origin at
   ~100s (HTTP 524 at 125s).** Our 30s cap is not the binding limit. No
   caller can wait longer, however patient.
4. **The work is cumulative and it warms.** The first read pays; later ones
   are cheap. On a 23.9M-appearance monitor, four attempts capped at 30s
   each: `30s, 30s, 30s, then 200 in 9.2s`. On 30.6M: `524 at 125s`, then
   33.8s, then 7.3s. The index becomes answerable — nobody was ever letting
   it finish.

### SHIPPED — the background warm

`services/chifra/warmIndex.ts`. When a request-bound index read TIMES OUT, we
start the same read again with a 120s budget, outside the request, and drop
the result. The reader gets the 503 and a Retry; the retry lands on a warm
index.

**Where it earns its place, measured.** Below about 16M appearances the warm
changes nothing: one abandoned 30s attempt already warms the index, and old
and new code both answer on the second call (8.2s and 9.2s, two untouched
~15.5M monitors, same one-call/150s/one-call timeline). Higher up, one slice
is not enough — a 23.9M monitor read under 30s-only slices went
`30s, 30s, 30s, then 200 in 9.2s`. That is four page loads, and nothing in
the product told the reader to keep trying. The warm does that work in one
background pass.

Restraint is the whole design: one warm per (chain, address), at most 2 at
once, a 5-minute cooldown after each. Chain-scoped like every other cache
here.

**Only a timeout earns a warm** (`isIndexTimeout`). If chifra cannot be
reached, a longer read fails the same way, so we neither spend the daemon's
time nor promise a retry that cannot work. Proved both ways: a dead chifra
returns the plain 503 in **8.7ms**; one that accepts and hangs returns the
warming 503 at **30.0s**.

**And the promise expires.** After `HOPELESS_AFTER_FAILURES` failed warms on
one address the 503 drops the "try again" sentence. Telling a reader to retry
something that cannot work is the same lie as "no transactions found".

### STILL BROKEN — WPLS needs infrastructure we do not have

**224.7M appearances, a 1.797 GB monitor file.** It did not converge in three
consecutive ~125s reads, and its monitor file was byte-identical before and
after (1,797,444,160 bytes). The 71.6M monitor did not converge either
(524, 524, then 502 at 104s). The warm helps up to about 44M appearances and
no further.

The fix for those is on the chifra box, not in this repo: **keep the hot
monitors freshened out of band** (a scheduled `chifra list --silent` per
heavy address), and/or serve chifra from an origin without a ~100s edge
timeout. Once warm, the same reads take 2.7-10.4s — comfortably inside the
page's 40s deadline. Do NOT try to buy this back by raising
`ADDRESS_SECTION_TIMEOUT_MS`; the numbers are 3x past it.

## Last updated: 2026-08-25 EMPTY-MEANS-BROKEN SWEEP

Gate: 3 typechecks silent, lint clean, **api unit 746**, **web 2495 / 290
files**, **sdk 552**, **41 e2e**, production build clean.

**The theme of this whole session: an empty result is a CLAIM about the
world, and we were making it without proof.** It started with one bug — the
address page saying WPLS has no transactions — and the sweep that followed
found the same shape five more times, plus a signal that was measuring the
wrong thing entirely.

- **Four more empty-means-broken sites fixed** (internal transactions, token
  transfers, Slither's clean bill of health, and a fork simulation reporting
  "no state changes" after using the post-state as the pre-state). 30 sites
  triaged; 21 are honest best-effort degradation and were left alone.
- **The 4byte guess marker was measuring registrations, not doubt.**
  `transfer` and a mined `atInversebrah` both scored 6. Replaced with a
  vouched-signature table whose selectors are DERIVED by hashing.
- **`0x60806040` is not a selector** — it is the Solidity `PUSH1 0x80 PUSH1
  0x40` prologue, so every contract deployment was being given a method name
  off the front of its init bytecode.
- **A heavy address now warms its index out of band.** Helps to ~44M
  appearances; WPLS at 224.7M still needs work on the chifra box.

### 2026-08-25 CHECKLIST CLEARED + DEPLOYED

Gate green on the merged tree: 3 typechecks silent, lint clean, web **2490**
unit tests / 290 files, api unit **693**, sdk **552**, **41 e2e**, production
build clean.

Everything the checklist held is now done except two items that genuinely
need the user (both named under OPEN). Landed this session, on top of the
`f002f61` deploy:

- **The address page can no longer spin forever.** Three sections, three
  deadlines, three rendered states. Verified on PRODUCTION: the overview
  paints at t+1s and the page settles at ~31s, where it used to show a
  spinner for the whole 31 seconds.
- **And the backend stopped lying to it.** `getAddressTransactions` served a
  chifra timeout as `{transactions: [], total: 0}` with a 200, so the page
  said "No transactions found" about WPLS. It now returns a 503 and the page
  says so. Verified live: WPLS is a 503 at 30.6s, a normal address still
  returns its rows.
- **Every route fits a 375px screen.** `viewport.spec.ts`'s `KNOWN_FAILING`
  set is EMPTY for the first time — 19/19.
- **A 4byte name admits when it is a guess**, and recents link to the chain
  they were seen on.
- **The rail sees Uniswap V3 swaps**, which its `/swap/i` test never could.

### 2026-08-25 THREE MORE EMPTY-MEANS-BROKEN SITES, AND THE TRIAGE OF THE REST

The chifra fix has siblings. I audited all 30 `catch` blocks in
`packages/api/src` that swallow a failure into `[]`, `null` or `{}` and
asked one question of each: can this empty reach a reader as a fact about
the chain? Four could. The other 26 are best-effort enrichment where
"absent" is the honest answer, and they are unchanged.

**The four, all fixed the same way — find the second signal, extract a pure
exported predicate, tell the truth on the wire, test the distinguishing
case:**

- **`getInternalTransactions`** returned `[]` for a dead tracer, and the
  route piled `.catch(() => [])` and a 10s `[]` timeout on top. The tx page
  hides an empty section, so the reader concludes there were none. **A chain
  whose RPC has no `debug_*` namespace hits this on EVERY transaction** —
  which is most of them, since `DEBUG_RPC_URL` is chain 369 only. The signal
  is the trace root: `traceTransaction` never throws, it answers `trace:
  null` on total failure, and a plain value transfer traces fine and
  flattens to `[]`. `isTraceUnavailable()`.
- **`getTokenTransfers`** had the same shape. Its receipt read throws both
  for a pending hash (honest) and for a node that never answered. viem
  separates them: `TransactionReceiptNotFoundError` means the node replied
  with a null receipt. `isReceiptMissing()`.
- **`parseSlitherOutput`** turned unreadable output into `[]`, and
  `analyzeContract` published `detectorCount: 0, error: null`. **That is a
  clean bill of health for a contract nobody analysed.** Three signals from
  Slither itself: no `{`, JSON that will not parse, and an envelope whose
  own `success`/`error` say the run failed. A clean contract emits
  `results.detectors: []`, so a real empty survives.
  `isFailedSlitherRun()`.
- **`getPrestateTrace`** returned `{}`, so `collectStateDiff` used each
  post-state as its own pre-state and the simulator printed "No state
  changes detected." **A simulated transaction always spends gas**, so an
  empty prestate map is impossible from a working tracer — that
  impossibility is the whole signal. `isPrestateUnavailable()`.

**The wire now carries the distinction, and the UI reads it.** Three new
optional flags — `internalTransactionsAvailable`, `tokenTransfersAvailable`,
`stateDiffAvailable`. Optional is deliberate: TanStack Query persists to
IndexedDB, and a response cached before these existed must not paint a
phantom failure, so an ABSENT flag counts as available.

**A hidden section is the same lie said more quietly.** `TxDetail` only
rendered these sections when non-empty. A failed section now appears, open,
saying it could not load. Hiding it lets the reader conclude there were
none — which is exactly the conclusion we were trying to stop.

**Two things I looked at and deliberately left:**

- `monitor/matchers.ts` `matchBalanceThreshold` swallows a `getBalance`
  failure into "the alert did not fire". That is a real silent failure, but
  nothing renders it, and the outer `matchAlerts` already treats a throw
  identically (log, no alert). Fixing the inner catch changes nothing
  observable. **A user's balance alert can miss a block on an RPC blip and
  nobody is told.** The fix is an alert-health channel, not a predicate.
- `ForkSimulator/ResultPanels.tsx:71` sums a null `gasEstimate` as `0n`, so
  a bundle total silently understates when one leg could not be estimated.
  Web-side, outside this sweep.

**What a green suite did not catch, again.** Every one of these four had
passing tests. The tests asserted the SHAPE of the response — an array, a
count, a diff object — and every one of those assertions is true of a lie.
The new tests each pin the case where the two empties differ, and would fail
if the predicate were inverted.

**Not browser-verified.** The four failure states are hard to provoke
locally (they need a node without `debug_*`, a broken Slither, a fork
without `prestateTracer`). The new copy is exercised by jsdom render tests,
which is not the same thing as opening the page.

### 2026-08-24 A 4BYTE NAME NOW ADMITS WHEN IT IS A GUESS

**The scale, measured against production today.** 4byte.directory holds every
signature anyone ever registered, so one selector usually carries several. Of
the transactions that resolve to a name at all: **77% on chain 1** (3,348 of
4,351 over 40 blocks) and **47% on chain 369** (218 of 463 over 60 blocks)
have MORE THAN ONE candidate. We printed the first one as fact. Real names we
were serving: `ijekfhacdgb` for `0x00000012` on chain 1, `rz_16jun22_88961909`
and `razor_balance` on 369 — gas-token-era names, brute-forced so the selector
had leading zero bytes.

**`UNRESOLVABLE_SELECTORS` is the wrong tool for these.** Those selectors are
real and their names are legitimately registered. The set still holds only
`0x00000000`. Do not grow it to paper over a multi-candidate selector.

`summarizeMatches` / `lookupSelectorSummaries` in `services/signatures.ts`
return the first candidate plus `candidateCount`. That count is the ONLY thing
added to the wire — one integer per row, on `RecentTx.methodCandidates` and
`AddressTransaction.functionCandidates`. The alternatives are fetched per
selector from `/api/signatures/:selector`, and only when a reader hovers:
`Tooltip` mounts its `label` lazily, so the query inside it does not exist
until the bubble opens.

**At 77% the marker cannot shout.** A loud badge on three rows in four is
noise a reader learns to skip, and then it flags nothing. `MethodName` keeps
the name and adds a dotted underline plus a footnote-sized count. Verified in
the browser at 1280 and 375; no horizontal overflow at 375.

**Guard the missing count, not the low one.** `MethodName` tests `> 1`, never
`<= 1`. TanStack Query persists responses to IndexedDB with
`staleTime: Infinity`, so a browser hands the component rows with NO count at
all — and `undefined <= 1` is false, which would have marked every stale row
as a guess of `undefined` candidates. Seen live before the guard went in.

## 2026-08-24 RECENTS NAME THEIR CHAIN

`RecentEntity` now carries the chain the entry was seen on, and the recents
rail and the ⌘K palette both build their links through one function,
`recentEntityView.hrefFor`. The palette used to call `scanPath` itself; it
does not any more, so the two surfaces cannot drift.

Which kinds get a chain is a rule, not an oversight. A transaction, a contract
and a block name theirs. An **address stays bare**, because `/address/0x…`
with no prefix is the documented "every chain" route and an address is valid
on all of them.

**The legacy decision.** An entry written before this change has no chain and
there is no way to recover one. It stays unset and links bare — exactly what
shipped before — so a resolve+redirect still finds the right chain on click.
A stored `chainId` that is not a positive integer (`null`, `"369"`, NaN) is
STRIPPED at load, because keeping one would build `/eip155/NaN/tx/0x…`. Both
paths are pinned by tests in `recentEntities.test.ts`.

The chain is deliberately NOT part of the entry's identity (`idOf`). One
address is one row whichever chain you reached it from; a revisit on another
chain overwrites the field, so the link follows the newest visit.

## Last updated: 2026-08-25 GAS PAYLOAD + EXPLORER HOME REBUILD

Gate green: 3 typechecks silent, lint clean, web **2416 unit tests**, api unit
**687**, sdk **552**, **41 e2e**. **Production serves `f002f61`** — deployed
2026-08-25, blue -> green, all four chains `ready`. Verified live: the gas
oracle is 798-1,287 bytes per chain (was 647,932 on 369) and carries only the
ten client fields; tx `0x8b69a5...` returns `functionName: null` for its
`0x00000000` selector instead of a mined 4byte name.

### SHIPPED

**The gas oracle stopped shipping its own memory to every browser**
(`2b19c21`). `GET /api/gas/oracle` returned **647,932 bytes** on chain 369, of
which 1,158 were real. `ring` was 98.4% of it — twenty blocks, each holding
every transaction's tip. The home page polls it every five seconds, so one
open tab pulled ~7.4 MB a minute to render four numbers. The route did
`serialize(state)` on a PRODUCER state object; the package says four times
over that these fields are producer-local and publishers should strip them.
Now 1,259 bytes. **515x.**

**The explorer home page, rebuilt** (`963bbf1`, `3e30dd2`, `c42a515`, plus
three merged branches). An outage used to be invisible, both lists now have
real empty and error states, the page names its chain, and both lists sit on
`EntityRow`. Details in the commits.

**A transaction is no longer named after a mined 4byte collision** (`0f13199`)
and **Settings measures whether an endpoint serves history** (`3faa100`).

**Every route now fits a 375px screen.** `/` was 412px wide and
`/network-health` 580px. Both are 375px, `KNOWN_FAILING` in
`packages/web/e2e/viewport.spec.ts` is empty, and all 41 e2e tests pass. The
two causes are under HARD-WON below — neither is a clip.

### HARD-WON — DO NOT RE-DERIVE

- **A bare `catch { return [] }` told readers that WPLS has no history.**
  `listAppearances` swallowed every chifra failure into an empty array, so a
  30-second timeout was served to the browser as `{transactions: [], total: 0}`
  — a 200. The address page then printed "No transactions found" about one of
  the busiest contracts on chain 369. Measured: 30.2s, the chifra cap exactly.
  The signal to tell the two apart was already being computed one line above:
  `countAppearances` returns `null` ONLY on an outage, and a genuinely empty
  address reports a real `0`. `getMergedActivity` already knew this and made
  its own second call to work around it. `getAddressTransactions` now throws a
  503 on `isAppearanceOutage()`, so every caller gets the truth and the
  workaround is deleted. **An empty result is a claim about the world — make
  sure you can prove it before you serve it.**

- **A stale route interception outlives the page that installed it.** After
  testing the home page's outage state by forcing `**/api/**` to 500, that
  route stayed armed in the browser context. The next session opened
  PRODUCTION and saw "not updating - showing data 33m old" plus "Gas oracle
  unavailable" and nearly filed a deploy regression. The tell: `/health`
  came back with a `cf-ray` and `server: cloudflare`, while every `/api/*`
  reply had NO server header, no `cf-ray`, and a 2-byte `{}` body. A reply
  that never touched the network does not carry the edge's headers — check
  for them before believing an outage.
- **A Vite HMR reconnect resets React state and looks like a bug.** The
  address page's progress counter ran BACKWARD, 1/3 -> 0/3 -> 1/3, about
  five seconds after load. It correlates exactly with a second
  `[vite] connecting... connected` in the console: the HMR socket dropped,
  forced a reload, and remounted the tree. There is no HMR client in a
  production bundle. Before chasing a reset like this, log the `[vite]`
  console lines and check the timestamps line up.
- **`vite preview` is NOT a way to verify an API-backed page.** `server.proxy`
  is dev-only; preview needs its own `preview.proxy`. Without it every
  `/api` call 404s and every section fails, so the page you are inspecting
  is lying to you for a reason that has nothing to do with your change.

- **A "state" object from a package is not a wire format.** Check for a
  `toPublishable` before serializing one wholesale. Ours lived in the relay
  and was never called; nobody noticed for as long as the endpoint existed,
  because no client read the extra fields — they were just sent.
- **`truncate` is `white-space: nowrap` + `overflow: hidden`.** A `break-all`
  flex child inside it cannot wrap, collapses to nothing, and SPILLS its text
  across whatever is beside it. This produced two separate bugs in one hour: a
  method name overlapping the fee readout, and a 66-char hash running 196px
  past the content pane. One line holds one thing.
- **A grid item defaults to `min-width: auto`** and refuses to shrink below
  its content, so `break-all` on a child never fires. `min-w-0` on the item is
  what lets it act.
- **`viewport.spec.ts` was blind for its whole life.** It asserted only
  `document.documentElement.scrollWidth <= 375`, and `app-content` is a
  `flex-1 overflow-auto min-w-0` containment boundary that absorbs overflow
  internally — the document never moves off 375. Four of nineteen routes were
  overflowing while it passed: `/ui` 223px, `/network-health` 205px,
  `/explorer` 202px, `/` 37px. It asserts the pane now, all four are fixed,
  and `KNOWN_FAILING` is empty. Keep it empty.
- **An absolutely-positioned popover still grows its scroll container.**
  `InfoTip` on `/network-health` was `absolute left-0 w-72` inside its ⓘ
  trigger; a trigger near the right edge threw all 288px of bubble past the
  pane — the whole 205px. `visibility: hidden` does not help, because a hidden
  box is still laid out. It renders through a portal at `position: fixed` now,
  clamped on screen, the same trick `primitives/Tooltip` already used. Any
  hover panel measured by that gate needs the portal.
- **An `<input>` carries a browser intrinsic width (~173px)**, and a flex item
  defaults to `min-width: auto`, so a `flex-1` box holding one will not shrink.
  That was Landing's whole 37px: the search box held its ground and pushed the
  Go button off a 375px screen. `min-w-0` on the box AND on the input.
- **`test.fail()` goes red when a test PASSES.** That is the feature: fixing
  EntityRow and the gas strip fixed `/ui` as a side effect, and the gate said
  so without anyone thinking to check.
- **`--color-text-muted` was 3.77:1** on the card background against a 4.5:1
  floor, used 450 times. Now `#808996`, the darkest value on the same hue line
  that clears AA on every surface.
- **Two formatters named `formatGwei` disagreed in public.** Chain 1's stat
  tile said "0.11 gwei" beside a gas strip saying "base 0 gwei". One shared
  `formatGweiDisplay` now.
- **Parallel agents each start their own dev server.** Mine landed on 11802
  while an agent's worktree held 11801, and I screenshotted THEIR build and
  briefly believed my changes had not applied. Read the port out of the log
  every time.
- **`npx playwright test` collides with the repo's own install** ("did not
  expect test() to be called here"). Use `node_modules/.bin/playwright`.
- **`npm run test:unit --workspace=packages/api` needs the API on :10100.**
  Five `/api/tx/:hash` tests live under `tests/unit/` and hit the live server;
  without it they fail with a bare `createConnectionError`.
- **`npm run test` is NOT the tests.** It runs the API *integration* suite.
  Four suites: web vitest, api unit, api integration, Playwright.
- **jsdom does no layout.** Geometry belongs in e2e, always.
- **op hangs on a STALE `~/.config/op/op-daemon.sock`.** `op --version` works
  while `op whoami` hangs. Fix: `pkill -f "op daemon"; rm -f` the socket.
- **Two implementer subagents must not share a working tree.** Worktree
  isolation worked cleanly here — three branches, three merges, one conflict
  auto-resolved.

### DEPLOY

`yarn deploy --env rpc-otterscan --plan explore-deploy` from the monorepo.
`TRACE_HOLDINGS_GRAPHQL_SECRET` is EMPTY in the monorepo `.env` and must come
from `op://valve/valve_hasura_secret/password` on the command line each time;
the other five `TRACE_*` vars are populated and bun autoloads them. Blue/green;
rollback is `--plan explore-rollback`. Pushing selects the build.

### OPEN

- **`/` overflows 37px and `/network-health` 205px at 375px.** Both marked
  `test.fail()` in `viewport.spec.ts` with their measurements.
- **Spinner-forever in `AddressView`.** The backend does NOT hang — chifra
  bounds at 30s, viem at 30s. The defect is a 15–30s load with no feedback and
  no client-side defence. Needs per-call-site `AbortSignal.timeout` at **35–40s**
  (above the observed latency, NOT the 8–10s used for cheap lookups),
  `Promise.allSettled`, per-section degradation.
- **The rail's swap branch — SETTLED by measurement, see `e817d0b`.** The two
  old audits contradicted each other because each measured a DIFFERENT CHAIN.
  Re-measured 2026-08-25 against production, 40 blocks of chain 1 (11,454 txs)
  and 60 of chain 369 (4,265): direct swaps beat wrappers **117:100 on 369**,
  wrappers beat direct **350:186 on chain 1**. Both sit near 2-3% of all
  traffic either way. So neither audit was wrong; both were parochial.
  The real defect was elsewhere and cheaper: `/swap/i` could not see
  **30.3% of NAMED swaps on chain 1** (86 of 88 misses were Uniswap V3's
  `exactInputSingle`) and 12.0% on 369. Fixed with anchored semantic
  prefixes. The multicall gap stays OPEN on purpose — unwrapping needs
  argument-level calldata decoding for a minority of a minority. It is a
  product call, now pinned by a test that documents it as deliberate.
- **The home page shows no fee per transaction any more.** Deliberate: it did
  not survive a two-line row. If this audience wants it back, the mempool view
  is the place, not a scan list.
- **4byte guesses still render as facts** when a selector has several
  candidates. Needs a candidate count carried to the UI.
- **The `rpc-collector` dataset is refreshed and pushed** (evm-toolkit
  `7a53a57`). The source is at `evm-toolkit/packages/rpc-collector` — it WAS
  on this machine; an earlier note here said otherwise and was wrong. The
  vendored DefiLlama constants were snapshotted 2026-07-22, five days before
  our endpoints merged upstream, holding ZERO `valve.city` entries. After
  `refresh-vendor` + `generate:data`: **609 -> 981 chains, 3,000 endpoints**,
  both transports on 1/369/943. Sepolia stays 0 until its PR merges.
  Added the guard that was missing: every prior test checked the data was
  WELL-FORMED, none checked it was CURRENT — which is how 0.23.0 and 0.24.0
  both shipped byte-identical stale data with a green suite. Sepolia is
  pinned with `it.fails`, so it goes RED the moment the PR merges.
  **Not released.** `evm-toolkit/CLAUDE.md` forbids autonomous releases and
  the release line covers 14 packages. Version left at 0.24.0. And note
  `packages/web` pins `^0.23.0` — a caret on a `0.x` never crosses a minor,
  so trace sees none of this until that pin moves.
- **Sepolia in chainlist — prepared, needs the user to open the PR.** The
  "143 commits behind" question answered itself: our old branch
  `valve-rpc-privacy-none` was **entirely redundant**. Chains 1/369/943 are
  already in DefiLlama/main, and upstream carries `wss://` variants our
  branch never had. Branched fresh off `origin/main` as `valve-sepolia-rpc`
  and committed the Sepolia https+wss pair (chainlist `2e1c4c9`, UNPUSHED).
  Both transports verified live: `eth_chainId` -> `0xaa36a7`.
  The dataset half is separately blocked: **the `rpc-collector` SOURCE repo
  is not checked out on this machine**, and a version bump does not help —
  0.23.0 (installed) and 0.24.0 (latest) ship an IDENTICAL dataset, 609
  chains, **zero `valve.city` entries** on any of 1/369/943/11155111. It
  predates the July merge and was not regenerated between those releases.
  Needs a real collector run plus an npm publish.
- **`recentEntityView.ts` and `AppShell/buildResults.ts` build chain-less
  paths** — `RecentEntity` has no `chainId` field.
